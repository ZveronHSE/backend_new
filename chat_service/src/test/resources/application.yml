grpc:
  server:
    port: 0

server:
  port: 0

spring:
  main:
    banner-mode: off
  application:
    name: profile-service
  data:
    cassandra:
      contact-points: localhost
      port: 9042
      local-datacenter: datacenter1
      schema-action: none

platform:
  grpc:
    apigateway:
      metadata: true
    client:
      logging-enabled: true
    server:
      logging-enabled: true